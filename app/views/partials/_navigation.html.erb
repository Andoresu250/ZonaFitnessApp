<nav class="navbar navbar-custom">
  <div class="container">

    <!-- MOBILE-->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed " data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <i class="fa fa-bars"></i>
      </button>
      <%= link_to "Zona Fitness", root_path, :class => "navbar-brand" %>
    </div>    

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">

      <ul class="nav navbar-nav navbar-right">
        
        <% if user_signed_in? %>  

          <% if current_user.is_admin? %>
             <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Opciones de administrados<span class="caret"></span></a>
                <ul class="dropdown-menu">
                  <li> <%= link_to("Editar usuarios", "/admins") %> </li>
                </ul>
              </li>
          <% end %>

           
          <li><a href="#">Bienvenido <%= current_user.name.split(" ").first %></a></li>  
          
          <li><%= link_to "log out", destroy_user_session_path, method: :delete %></li>  

        <% else %>

          <li><%= link_to "log in", new_user_session_path %> </li>  
          <li><%= link_to "New User", new_user_registration_path %> </li>  

        <% end %>              
      </ul>
    </div>
  </div>
</nav>